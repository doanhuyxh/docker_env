services:
  postgresql:
    image: postgres:18
    container_name: postgresql
    ports:
      - "5432:5432"
    volumes:
      - ./postgresql_data:/var/lib/postgresql
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: pgpassword2025
      POSTGRES_DB: mydatabase
    restart: unless-stopped